{
  "name": "resume2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx light-server -s . -b 127.0.0.1 -p8080  -w '**/*.html, **/*.js, **/*.css' -o"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
