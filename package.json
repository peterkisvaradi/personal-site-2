{
  "name": "resume2",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx light-server -s . -b 127.0.0.1 -p8080  -w '**/*.html, **/*.js, **/*.css' -o",
    "build": "rm -rf dist && mkdir dist && parcel build index.html --dist-dir dist --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
